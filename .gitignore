.DS_Store
.gitignore
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix
.quartz/
quartz/cli/tui/dist/

# Symlink to the external Obsidian vault — never commit (dangles on CI runners,
# and vault source must stay private; see deploy.yaml for how content is fetched in CI)
/content

# Local design scratch, not part of the published site
frontend-mockup/

# Peer-dependency symlinks regenerated by `quartz plugin add/install`
# inside local plugin overrides (see local-plugins/)
local-plugins/*/node_modules
